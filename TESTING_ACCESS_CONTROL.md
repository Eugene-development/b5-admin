# Инструкция по тестированию системы доступов

## Подготовка к тестированию

### 1. Создание тестовых пользователей

Выполните следующие SQL-запросы для создания тестовых пользователей с разными статусами:

```sql
-- Получить ID статуса "Админ"
SELECT id, slug, value FROM user_statuses WHERE slug = 'admin';

-- Создать тестового администратора
INSERT INTO users (id, key, name, email, password, status_id, email_verified_at, created_at, updated_at)
VALUES (
    ULID(),
    ULID(),
    'Test Admin',
    'admin@test.com',
    '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', -- password: password
    (SELECT id FROM user_statuses WHERE slug = 'admin'),
    NOW(),
    NOW(),
    NOW()
);

-- Создать тестового менеджера
INSERT INTO users (id, key, name, email, password, status_id, email_verified_at, created_at, updated_at)
VALUES (
    ULID(),
    ULID(),
    'Test Manager',
    'manager@test.com',
    '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', -- password: password
    (SELECT id FROM user_statuses WHERE slug = 'managers'),
    NOW(),
    NOW(),
    NOW()
);

-- Создать тестового агента
INSERT INTO users (id, key, name, email, password, status_id, email_verified_at, created_at, updated_at)
VALUES (
    ULID(),
    ULID(),
    'Test Agent',
    'agent@test.com',
    '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', -- password: password
    (SELECT id FROM user_statuses WHERE slug = 'agents'),
    NOW(),
    NOW(),
    NOW()
);
```

## Тестирование доступа администратора

### Тест 1: Вход с домена admin.bonus.band

1. Откройте браузер и перейдите на `http://localhost:5137` (или ваш локальный URL)
2. Войдите с учетными данными:
   - Email: `admin@test.com`
   - Password: `password`
3. После входа откройте консоль браузера и выполните:
   ```javascript
   // Импортировать функцию отладки (в консоли браузера)
   import { debugDomainState } from '/src/lib/utils/domainAccess.svelte.js';
   debugDomainState();
   ```
4. Проверьте вывод:
   - `user.status.slug` должен быть `'admin'`
   - `hasAdminAccess` должен быть `true`
   - Все элементы `navigationVisibility` должны быть `true`

### Тест 2: Проверка доступа ко всем страницам

Администратор должен иметь доступ ко всем страницам. Перейдите по следующим ссылкам и убедитесь, что НЕТ редиректа на `/access-denied`:

**Менеджмент:**
- [/agents](http://localhost:5137/agents)
- [/curators](http://localhost:5137/curators)
- [/managers](http://localhost:5137/managers)
- [/designers](http://localhost:5137/designers)
- [/clients](http://localhost:5137/clients)

**Контрагенты:**
- [/contractors](http://localhost:5137/contractors)
- [/suppliers](http://localhost:5137/suppliers)
- [/delivery](http://localhost:5137/delivery)
- [/services](http://localhost:5137/services)

**Бизнес-процессы:**
- [/projects](http://localhost:5137/projects)
- [/tz](http://localhost:5137/tz)
- [/contracts](http://localhost:5137/contracts)
- [/order](http://localhost:5137/order)
- [/complaints](http://localhost:5137/complaints)
- [/finance](http://localhost:5137/finance)

**Информация:**
- [/actions](http://localhost:5137/actions)
- [/documentation](http://localhost:5137/documentation)

### Тест 3: Проверка видимости навигации

1. Откройте боковое меню (sidebar)
2. Убедитесь, что **ВСЕ** пункты меню видимы:
   - ✅ Дашборд
   - ✅ Клиенты
   - ✅ Агенты
   - ✅ Кураторы
   - ✅ Менеджеры
   - ✅ Дизайнеры
   - ✅ Подрядчики
   - ✅ Поставщики
   - ✅ Доставка
   - ✅ Сервис
   - ✅ Проекты
   - ✅ Техзадания
   - ✅ Контракты
   - ✅ Закупка
   - ✅ Рекламации
   - ✅ Акции
   - ✅ Документация

## Тестирование ограниченного доступа (менеджер)

### Тест 4: Вход менеджера

1. Выйдите из системы
2. Войдите с учетными данными менеджера:
   - Email: `manager@test.com`
   - Password: `password`
3. Проверьте консоль:
   - `user.status.slug` должен быть `'managers'`
   - `hasAdminAccess` должен быть `false`

### Тест 5: Проверка ограниченного доступа

**Менеджер ДОЛЖЕН иметь доступ к:**
- ✅ `/dashboard`
- ✅ `/projects`
- ✅ `/clients`
- ✅ `/actions`
- ✅ `/order`
- ✅ `/tz`
- ✅ `/documentation`

**Менеджер НЕ должен иметь доступ к (должен редиректить на `/access-denied`):**
- ❌ `/finance`
- ❌ `/contractors`
- ❌ `/suppliers`
- ❌ `/delivery`
- ❌ `/services`
- ❌ `/agents` (управление агентами)
- ❌ `/curators`
- ❌ `/managers` (управление менеджерами)
- ❌ `/designers`

### Тест 6: Проверка видимости навигации для менеджера

В боковом меню должны быть видны **только** следующие пункты:
- ✅ Дашборд
- ✅ Клиенты
- ✅ Проекты
- ✅ Техзадания
- ✅ Закупка
- ✅ Акции
- ✅ Документация

**НЕ должны быть видны:**
- ❌ Агенты
- ❌ Кураторы
- ❌ Менеджеры
- ❌ Дизайнеры
- ❌ Подрядчики
- ❌ Поставщики
- ❌ Доставка
- ❌ Сервис
- ❌ Контракты (если не в списке разрешений)
- ❌ Рекламации (если не в списке разрешений)

## Тестирование минимального доступа (агент)

### Тест 7: Вход агента

1. Выйдите из системы
2. Войдите с учетными данными агента:
   - Email: `agent@test.com`
   - Password: `password`

### Тест 8: Проверка минимального доступа

**Агент ДОЛЖЕН иметь доступ к:**
- ✅ `/dashboard`
- ✅ `/projects`
- ✅ `/actions`
- ✅ `/documentation`

**Агент НЕ должен иметь доступ к (должен редиректить на `/access-denied`):**
- ❌ Все остальные страницы

### Тест 9: Проверка видимости навигации для агента

В боковом меню должны быть видны **только**:
- ✅ Дашборд
- ✅ Проекты
- ✅ Акции
- ✅ Документация

## Тестирование multi-domain доступа

### Тест 10: Администратор с разных доменов

Убедитесь, что администратор имеет полный доступ с любого разрешенного домена:

1. **Localhost:**
   - URL: `http://localhost:5137`
   - Войдите как `admin@test.com`
   - Проверьте полный доступ ко всем страницам

2. **admin.bonus.band (production):**
   - URL: `https://admin.bonus.band`
   - Войдите как администратор
   - Проверьте полный доступ ко всем страницам

3. **rubonus.pro (production):**
   - URL: `https://rubonus.pro` или аналогичный
   - Войдите как администратор
   - Проверьте полный доступ ко всем страницам

## Автоматическое тестирование (опционально)

Создайте тестовый скрипт для автоматической проверки доступа:

```javascript
// test-access.js
import { hasRouteAccess } from './src/lib/auth/status-permissions.js';

const tests = [
  // Админ должен иметь доступ ко всем маршрутам
  { status: 'admin', route: '/finance', expected: true },
  { status: 'admin', route: '/contractors', expected: true },
  { status: 'admin', route: '/any-route', expected: true },

  // Менеджер - ограниченный доступ
  { status: 'managers', route: '/projects', expected: true },
  { status: 'managers', route: '/clients', expected: true },
  { status: 'managers', route: '/finance', expected: false },
  { status: 'managers', route: '/contractors', expected: false },

  // Агент - минимальный доступ
  { status: 'agents', route: '/projects', expected: true },
  { status: 'agents', route: '/actions', expected: true },
  { status: 'agents', route: '/finance', expected: false },
  { status: 'agents', route: '/clients', expected: false },
];

tests.forEach(test => {
  const result = hasRouteAccess(test.status, test.route);
  const passed = result === test.expected;
  console.log(
    passed ? '✅' : '❌',
    `${test.status} -> ${test.route}:`,
    `expected ${test.expected}, got ${result}`
  );
});
```

## Отладка проблем

### Проблема: Навигация не обновляется после входа

**Решение:**
1. Проверьте, что `authState` правильно инициализирован
2. Проверьте, что данные пользователя содержат `status.slug`
3. Проверьте консоль на наличие ошибок
4. Выполните `debugDomainState()` в консоли

### Проблема: Администратор не имеет доступа к странице

**Решение:**
1. Проверьте статус пользователя в базе данных:
   ```sql
   SELECT u.email, u.name, s.slug, s.value
   FROM users u
   JOIN user_statuses s ON u.status_id = s.id
   WHERE u.email = 'admin@test.com';
   ```
2. Убедитесь, что `slug = 'admin'`
3. Проверьте `STATUS_PERMISSIONS` в `status-permissions.js`

### Проблема: Редирект на `/access-denied` для разрешенного маршрута

**Решение:**
1. Проверьте конфигурацию разрешений в `status-permissions.js`
2. Убедитесь, что маршрут добавлен в массив `routes` для соответствующего статуса
3. Проверьте, что маршрут точно совпадает (с учетом регистра и слэшей)

## Чеклист успешного тестирования

- [ ] Администратор входит с localhost
- [ ] Администратор имеет доступ ко всем страницам
- [ ] Администратор видит все пункты меню
- [ ] Менеджер имеет ограниченный доступ
- [ ] Менеджер видит только разрешенные пункты меню
- [ ] Агент имеет минимальный доступ
- [ ] Агент видит только базовые пункты меню
- [ ] Серверная проверка работает (попытка прямого доступа к запрещенной странице редиректит)
- [ ] Навигация обновляется после входа/выхода
- [ ] `debugDomainState()` выводит корректные данные

## Готово!

Если все тесты пройдены успешно, система доступов работает корректно! 🎉
