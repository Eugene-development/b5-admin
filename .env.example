# B5-Admin Environment Configuration
# These variables are optional - the app uses a config system with fallbacks

# Development API endpoints (automatically used in development)
# API_BASE_URL=http://localhost:8000
# AUTH_API_URL=http://localhost:8001
# FRONTEND_URL=http://localhost:5137

# Production API endpoints (set by Docker secrets in production)
# API_BASE_URL=https://api.bonus.band
# AUTH_API_URL=https://auth.bonus5.ru
# FRONTEND_URL=https://admin.bonus.band

# For production deployment with Docker, these are handled by entrypoint.sh
# PUBLIC_API_BASE_URL=
# PUBLIC_AUTH_API_URL=
# PUBLIC_FRONTEND_URL=