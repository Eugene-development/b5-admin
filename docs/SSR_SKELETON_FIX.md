# Исправление проблемы с вечным скелетоном на страницах Договора и Заказы

## Проблема

На страницах Договора (`/contracts`) и Заказы (`/order`) отображался вечный скелетон, данные не загружались.

## Причина

**Главная причина:** SSR был отключен глобально в `src/routes/+layout.js`:

```javascript
// ❌ Неправильно - SSR отключен
export const ssr = false;
```

Это означало, что:
1. Файлы `+page.server.js` вообще не выполнялись
2. Данные не загружались на сервере
3. Компоненты ждали данные, которые никогда не приходили

```javascript
// ❌ Неправильно - возвращает разрешенные данные
export async function load({ locals, fetch }) {
    const ordersData = await loadOrdersData(locals.token, fetch);
    return { ordersData }; // ordersData - это объект, а не промис
}
```

В компоненте:
```svelte
{#await data.ordersData}  <!-- Ожидает промис, но получает объект -->
    <TableSkeleton />
{:then ordersData}
    <!-- Никогда не выполняется -->
{/await}
```

## Решение

### Шаг 1: Включить SSR глобально

Изменить `src/routes/+layout.js`:

```javascript
// ✅ Правильно - SSR включен
export const ssr = true;
```

**Почему это безопасно:**
- Проект уже мигрировал на httpOnly cookies (см. `hooks.server.js`)
- Токены больше не хранятся в localStorage
- SSR теперь имеет доступ к токенам через `event.cookies`

### Шаг 2: Убрать `{#await}` из компонентов (опционально)

Если используется `{#await}`, можно упростить код:

```svelte
{@const ordersData = data.ordersData}

{#if ordersData.error}
    <!-- Показать ошибку -->
{:else if isLoading}
    <TableSkeleton />
{:else}
    <!-- Показать данные -->
{/if}
```

## Применённые изменения

1. **b5-admin/src/routes/+layout.js** ⭐ ГЛАВНОЕ ИЗМЕНЕНИЕ
   - Изменено `export const ssr = false` на `export const ssr = true`
   - Это включило SSR для всего приложения

2. **b5-admin/src/routes/(protected)/(business-processes)/order/+page.svelte**
   - Удален блок `{#await data.ordersData}...{/await}`
   - Заменен на прямую работу с данными через `{@const ordersData = data.ordersData}`
   - Упрощена логика отображения ошибок и скелетона

3. **b5-admin/src/routes/(protected)/(business-processes)/contracts/+page.svelte**
   - Аналогичные изменения - удален `{#await}` блок

4. **b5-admin/src/routes/(protected)/(business-processes)/order/+page.server.js**
   - Добавлено логирование для отладки
   - Добавлена обработка ошибок с try-catch

5. **b5-admin/src/routes/(protected)/(business-processes)/contracts/+page.server.js**
   - Аналогичные изменения

6. **b5-admin/src/routes/(protected)/(business-processes)/order/+page.js**
   - Создан файл с настройками SSR:
   ```javascript
   export const ssr = true;
   export const prerender = false;
   ```

## Проверка

После изменений:

1. **Перезапустите dev-сервер** (важно! Изменение SSR требует перезапуска)
2. Откройте страницу заказов/договоров
3. Проверьте терминал сервера - должны быть логи:
   - `🚀 Orders SSR: load function called`
   - `✅ Orders SSR: Returning data`
4. Данные должны отображаться сразу, без вечного скелетона

## Важно

- После включения SSR необходимо **перезапустить dev-сервер**
- Простое обновление страницы в браузере не поможет
- Убедитесь, что в консоли браузера нет ошибок

## Дополнительная информация

- SvelteKit автоматически обрабатывает `await` в функции `load`
- Блок `{#await}` работает только с промисами
- Для SSR данные должны загружаться на сервере через `+page.server.js`
- Файл `+page.js` с `ssr: true` обеспечивает серверный рендеринг
