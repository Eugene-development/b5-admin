# Быстрое исправление ошибки аутентификации на странице Agents

## Проблема

На продакшене страница `/agents` выдает ошибку "Unauthenticated", а `/clients` работает.

## Причина

Запрос `users` в GraphQL требует JWT аутентификацию (`@guard`), возможно закеширована старая конфигурация.

## Быстрое решение

### На продакшн сервере:

```bash
# 1. SSH на сервер
ssh user@your-server

# 2. Перейти в директорию b5-api-2
cd /var/www  # или ваш путь к b5-api-2

# 3. Очистить все кеши
php artisan config:clear
php artisan cache:clear
php artisan lighthouse:clear-cache

# 4. Пересоздать кеш конфигурации
php artisan config:cache

# 5. Перезапустить PHP-FPM
sudo systemctl restart php8.2-fpm  # или php8.1-fpm, php8.3-fpm
```

### На локальной машине:

```bash
# 1. Задеплоить изменения с логированием
cd b5-admin
git add .
git commit -m "Add JWT auth debugging for agents page"
git push

# 2. Задеплоить на продакшн (ваш процесс деплоя)
```

## Проверка

1. Откройте https://admin.bonus.band
2. Войдите в систему
3. Откройте DevTools (F12) → Console
4. Перейдите на страницу `/agents`
5. Проверьте логи:
   - ✅ Если видите `🔐 [refreshUsers] Auth headers: { Authorization: "Bearer ..." }` - токен передается
   - ❌ Если видите `🔐 [refreshUsers] Auth headers: {}` - токен не передается

## Если не помогло

См. полную документацию: `b5-admin/docs/JWT_AGENTS_AUTH_FIX.md`
