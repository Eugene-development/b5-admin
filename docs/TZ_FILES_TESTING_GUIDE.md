# Руководство по тестированию отображения файлов ТЗ

## Дата: 6 декабря 2025

## Цель

Проверить, что файлы техзаданий (эскизы и коммерческие предложения) корректно загружаются и отображаются в модальном окне просмотра.

## Предварительные требования

1. Backend (b5-api-2) запущен и доступен
2. Frontend (b5-admin) запущен
3. Пользователь авторизован в системе
4. Есть хотя бы одно техзадание в базе данных

## Шаги тестирования

### 1. Проверка загрузки файла

1. Откройте страницу `/tz` в браузере
2. Найдите техзадание в таблице
3. Нажмите кнопку "Загрузить эскиз" (иконка изображения)
4. Выберите файл изображения (PNG, JPG, JPEG, GIF, WEBP, SVG)
5. Нажмите "Загрузить"
6. Дождитесь сообщения об успешной загрузке

**Ожидаемый результат:**
- Появляется toast-уведомление "Эскиз успешно загружен"
- Модальное окно загрузки закрывается
- Данные в таблице обновляются

### 2. Проверка отображения в модальном окне просмотра

1. Нажмите кнопку "Просмотр" (иконка глаза) на том же техзадании
2. Прокрутите вниз до секции "Файлы техзадания"
3. Проверьте секцию "Эскизы"

**Ожидаемый результат:**
- Отображается количество эскизов: "Эскизы (1)" или больше
- Видна карточка файла с:
  - Иконкой изображения (синяя)
  - Названием файла
  - Размером файла в MB
  - Именем загрузившего пользователя
  - Кнопкой "Скачать"

### 3. Проверка загрузки КП

1. Закройте модальное окно просмотра
2. Нажмите кнопку "Загрузить КП" (иконка документа)
3. Выберите файл документа (PDF, DOC, DOCX, XLS, XLSX)
4. Нажмите "Загрузить"
5. Дождитесь сообщения об успешной загрузке

**Ожидаемый результат:**
- Появляется toast-уведомление "КП успешно загружено"
- Модальное окно загрузки закрывается

### 4. Проверка отображения КП в модальном окне

1. Снова откройте модальное окно просмотра
2. Прокрутите до секции "Коммерческие предложения"

**Ожидаемый результат:**
- Отображается количество КП: "Коммерческие предложения (1)" или больше
- Видна карточка файла с:
  - Иконкой документа (фиолетовая)
  - Названием файла
  - Размером файла в MB
  - Именем загрузившего пользователя
  - Кнопкой "Скачать"

### 5. Проверка скачивания файла

1. Нажмите кнопку "Скачать" на любом файле
2. Проверьте, что файл начал скачиваться

**Ожидаемый результат:**
- Файл скачивается с правильным именем
- Файл открывается корректно

### 6. Проверка обновления данных при открытом модальном окне

1. Откройте модальное окно просмотра техзадания
2. Не закрывая модальное окно, откройте страницу в новой вкладке
3. В новой вкладке загрузите еще один файл для того же техзадания
4. Вернитесь в первую вкладку с открытым модальным окном
5. Обновите страницу (F5)
6. Снова откройте модальное окно просмотра

**Ожидаемый результат:**
- Новый файл отображается в списке
- Счетчик файлов обновлен

## Проверка через GraphQL Playground

### Запрос для получения техзаданий с файлами

```graphql
query GetTechnicalSpecifications {
  technicalSpecifications(first: 10) {
    data {
      id
      value
      project_id
      description
      comment
      is_active
      requires_approval
      is_approved
      sketches {
        id
        file_type
        file_name
        file_path
        file_size
        mime_type
        uploaded_by
        uploader {
          id
          name
          email
        }
        created_at
        updated_at
      }
      commercialOffers {
        id
        file_type
        file_name
        file_path
        file_size
        mime_type
        uploaded_by
        uploader {
          id
          name
          email
        }
        created_at
        updated_at
      }
      created_at
      updated_at
    }
  }
}
```

### Мутация для загрузки файла

```graphql
mutation UploadTzFile($input: UploadTzFileInput!) {
  uploadTzFile(input: $input) {
    id
    technical_specification_id
    file_type
    file_name
    file_path
    file_size
    mime_type
    uploaded_by
    uploader {
      id
      name
      email
    }
    created_at
    updated_at
  }
}
```

**Variables:**
```json
{
  "input": {
    "technical_specification_id": "01JEXAMPLE123456",
    "file_type": "SKETCH",
    "file": null
  }
}
```

## Проверка логов

### Backend логи (b5-api-2)

Проверьте файл `storage/logs/laravel.log`:

```bash
cd b5-api-2
tail -f storage/logs/laravel.log | grep -i "UploadTzFile\|TzFileUploadService"
```

**Ожидаемые записи:**
```
[2025-12-06 12:00:00] local.INFO: UploadTzFile mutation called {"tz_id":"01JE...","file_type":"sketch","user_id":"01JE...","file_name":"example.png"}
[2025-12-06 12:00:00] local.INFO: File validation {"file_name":"example.png","file_type":"sketch","mime_type":"image/png","file_size":1234567,"allowed_types":["image/jpeg","image/jpg","image/png",...]}
```

### Frontend логи (Browser Console)

Откройте DevTools (F12) и проверьте консоль:

```javascript
// При загрузке файла
🔧 Uploading TZ file: {tzId: "01JE...", fileType: "SKETCH", fileName: "example.png", fileSize: 1234567, mimeType: "image/png"}
✅ Upload successful: {id: "01JE...", file_name: "example.png", ...}

// При обновлении данных
🔧 GraphQL Request Debug: {operationName: "getTechnicalSpecifications", endpoint: "...", variables: {...}, timestamp: "..."}
```

## Возможные проблемы и решения

### Проблема 1: Файлы не отображаются после загрузки

**Причина:** `selectedTz` не обновляется после загрузки файла

**Решение:** Проверьте, что в `+page.svelte` есть код обновления `selectedTz`:

```javascript
if (isViewModalOpen && selectedTz && selectedTz.id === tzId) {
    const updatedTz = tzList.find(t => t.id === tzId);
    if (updatedTz) {
        selectedTz = updatedTz;
    }
}
```

### Проблема 2: Ошибка "File type not allowed"

**Причина:** Неправильный MIME-type файла

**Решение:** 
- Для эскизов: используйте PNG, JPG, JPEG, GIF, WEBP, SVG
- Для КП: используйте PDF, DOC, DOCX, XLS, XLSX

### Проблема 3: Файлы не скачиваются

**Причина:** Неправильный URL файла в S3

**Решение:** Проверьте, что URL формируется правильно:
```javascript
`https://storage.yandexcloud.net/${sketch.file_path}`
```

### Проблема 4: Ошибка 401 Unauthorized при загрузке

**Причина:** Пользователь не авторизован

**Решение:** Проверьте, что cookie `b5_auth_2_session` установлена и валидна

## Критерии успешного прохождения тестов

- ✅ Файлы успешно загружаются
- ✅ Файлы отображаются в модальном окне просмотра сразу после загрузки
- ✅ Счетчики файлов обновляются корректно
- ✅ Файлы можно скачать
- ✅ Отображается информация о загрузившем пользователе
- ✅ Отображается размер файла
- ✅ Нет ошибок в консоли браузера
- ✅ Нет ошибок в логах backend

## Связанные документы

- `TZ_FILES_DISPLAY_FIX.md` - Описание исправления
- `TZ_FILE_UPLOAD_FIX.md` - Исправление загрузки файлов (если есть)
