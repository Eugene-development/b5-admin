# Функционал редактирования и удаления выплат

## Описание

Добавлен функционал для администраторов по редактированию и удалению заявок на выплату бонусов на странице "Выплаты" в b5-admin.

## Возможности

### Редактирование выплаты

Администратор может изменить следующие поля заявки:
- **Сумма выплаты** (минимум 1 000 ₽)
- **Способ выплаты** (Карта, СБП, Другое)
- **Реквизиты** (в зависимости от способа выплаты):
  - Номер карты (для карты)
  - Номер телефона (для СБП)
  - Контактная информация (для других способов)
- **Комментарий**

#### Ограничения редактирования:
- Нельзя редактировать заявки в статусе "Выплачено" (paid)
- Нельзя редактировать заявки в статусе "Отменено" (cancelled)

### Удаление выплаты

Администратор может удалить заявку на выплату.

#### Ограничения удаления:
- Нельзя удалить заявку в статусе "Выплачено" (paid)
- При удалении автоматически удаляются все связанные записи о покрытии бонусов

## Интерфейс

### Страница выплат

На странице `/payments` в таблице выплат добавлены кнопки:
- 👁️ **Просмотр** - открывает модальное окно с детальной информацией
- ✏️ **Редактировать** - открывает модальное окно редактирования
- 🗑️ **Удалить** - открывает модальное окно подтверждения удаления

### Модальное окно редактирования

Форма редактирования содержит:
- Поле ввода суммы с валидацией (минимум 1 000 ₽)
- Выбор способа выплаты (визуальные кнопки)
- Поля для реквизитов (динамически меняются в зависимости от способа)
- Текстовое поле для комментария
- Кнопки "Отмена" и "Сохранить"

### Модальное окно удаления

Диалог подтверждения показывает:
- Предупреждение о необратимости действия
- Информацию о заявке (агент, сумма, статус)
- Кнопки "Отмена" и "Удалить"

## API

### GraphQL мутации

#### updateBonusPaymentRequest

Обновляет данные заявки на выплату.

```graphql
mutation UpdateBonusPaymentRequest($requestId: ID!, $input: UpdateBonusPaymentRequestInput!) {
  updateBonusPaymentRequest(request_id: $requestId, input: $input) {
    id
    amount
    payment_method
    card_number
    phone_number
    contact_info
    comment
    status {
      id
      code
      name
    }
    agent {
      id
      name
      email
    }
  }
}
```

**Входные параметры:**
```graphql
input UpdateBonusPaymentRequestInput {
  amount: Float
  payment_method: String
  card_number: String
  phone_number: String
  contact_info: String
  comment: String
}
```

#### deleteBonusPaymentRequest

Удаляет заявку на выплату.

```graphql
mutation DeleteBonusPaymentRequest($requestId: ID!) {
  deleteBonusPaymentRequest(request_id: $requestId)
}
```

**Возвращает:** `Boolean` (true при успешном удалении)

## Файлы

### Frontend (b5-admin)

**API:**
- `src/lib/api/bonusPayments.js` - добавлены функции `updateBonusPaymentRequest()` и `deleteBonusPaymentRequest()`

**Компоненты:**
- `src/lib/components/finances/PaymentEditModal.svelte` - модальное окно редактирования
- `src/lib/components/finances/PaymentDeleteModal.svelte` - модальное окно удаления
- `src/lib/components/finances/PaymentsTable.svelte` - обновлена таблица с кнопками действий

**Страницы:**
- `src/routes/(protected)/(finances)/payments/+page.svelte` - добавлены обработчики обновления и удаления

### Backend (b5-api-2)

**GraphQL схема:**
- `graphql/bonus_payment_request.graphql` - добавлены мутации и input типы

**Resolvers:**
- `app/GraphQL/Mutations/UpdateBonusPaymentRequest.php` - обработка обновления заявки
- `app/GraphQL/Mutations/DeleteBonusPaymentRequest.php` - обработка удаления заявки

## Безопасность

- Операции доступны через GraphQL API
- Валидация данных на frontend и backend
- Защита от удаления/редактирования выплаченных заявок
- Транзакционное удаление с очисткой связанных данных
- Рекомендуется настроить проверку прав на уровне middleware или GraphQL директив

## Уведомления

Система показывает toast-уведомления:
- ✅ "Заявка успешно обновлена" - при успешном редактировании
- ✅ "Заявка успешно удалена" - при успешном удалении
- ❌ Сообщения об ошибках при неудачных операциях

## Тестирование

Для тестирования функционала:

1. Войдите в систему как администратор
2. Перейдите на страницу "Выплаты" (`/payments`)
3. Найдите заявку в статусе "Новая" или "В обработке"
4. Нажмите кнопку "Редактировать" (✏️)
5. Измените данные и сохраните
6. Проверьте, что изменения отобразились в таблице
7. Нажмите кнопку "Удалить" (🗑️)
8. Подтвердите удаление
9. Проверьте, что заявка исчезла из списка

## Известные ограничения

- Нельзя изменить агента, которому принадлежит заявка
- Нельзя изменить тип запрашивающего (agent/curator)
- Нельзя изменить статус через форму редактирования (для этого используется отдельный функционал смены статуса)
- При изменении способа выплаты старые реквизиты автоматически очищаются
